---
import ProductHeader from "../../../components/products/ProductHeader.astro";
import Layout from "../../../layouts/Layout.astro";
import BUS_DATA from "../../../docs/bussiness-data.json";
import ProductCategoriesList from "../../../components/products/ProductCategoriesList.astro";
import ProductCompleteList from "../../../components/products/ProductCompleteList.astro";

const currentCategory: string = Astro.url.searchParams.get("cat") || "TODOS";

const title: string = `Catálogo de productos · ${BUS_DATA.bussiness_name}`;
---

<Layout title={title} description="Todos nuestros productos">
    <div class="flex min-h-screen w-full flex-col font-sans text-gray-800">
        <ProductHeader />
        <ProductCategoriesList
            categories={BUS_DATA.products_categories}
            currentCategory={currentCategory}
        />
        <div class="w-full bg-amber-300 p-6">
            <h2 class="text-2xl font-bold">
                Esta web está en desarrollo. Los productos y precios no
                corresponden con los reales.
            </h2>
        </div>
        <ProductCompleteList currentCategory={currentCategory} />
    </div>
</Layout>
