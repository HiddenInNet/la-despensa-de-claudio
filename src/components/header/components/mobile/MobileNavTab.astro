---
interface Props {
    href: string;
    title: string;
}

const { href, title } = Astro.props as Props;
---

<li class="flex items-center justify-start px-6">
    <a
        href={`${href}`}
        class="flex w-full items-center gap-2 rounded-xl p-4 text-neutral-800 focus:bg-neutral-200"
    >
        <slot />
        <h3 class="rounded-xl text-lg text-nowrap">{title}</h3>
    </a>
</li>
