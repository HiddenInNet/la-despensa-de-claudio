---
import ProductCard from "./ProductCard.astro";
import { supabase } from "../../db/supabase";
import type { ProductDetail } from "../../types/products";

interface Props {
    category: string;
}

const { category = "TODOS" } = Astro.props;

let query = supabase.from("products").select("*");

if (category !== "TODOS") {
    query = query.eq("category", category);
}

const { data, error } = await query;

if (error) {
    console.error("Error obteniendo productos:", error.message);
}

const productsFiltered: ProductDetail[] = data ? (data as ProductDetail[]) : [];
---

<div class="grid w-full grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-3">
    {
        productsFiltered && productsFiltered.length > 0 ? (
            productsFiltered.map((product) => <ProductCard product={product} />)
        ) : (
            <p class="col-span-full py-12 text-center font-light text-stone-500">
                No se encontraron productos en esta categoría.
            </p>
        )
    }
</div>
